import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class Evaluation extends Component {
    render() {
        return (
            <View>
                <Text> Survey here ! </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
